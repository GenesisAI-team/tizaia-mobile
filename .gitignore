# Secretos — nunca commitear (los workers los reciben por symlink de AO)
.env.local
.env

# Artefactos del backend (API-001)
/backend/node_modules/
/backend/dist/
/backend/coverage/
# Artefactos locales del benchmark (issue #104) — nunca se versionan salvo
# fixtures deliberados.
/backend/.bench/

/design/
